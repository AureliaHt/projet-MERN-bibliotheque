import LogForm from "../components/logForm/LogForm";

const Home = () => {
    return (
        <div className="home">
            <div className="log-container">
                < LogForm />
            </div>
        </div>
    );
};

export default Home;